# Interfaces

- `conformance.Runner.Run` accepts profile, target, spec version, groups and an injected signer.
- `ReleaseEvidence` is validated by the release JSON Schema and can be independently verified.
- CLI command: `aor-conformance run --profile ... --spec-version ... --output ...`.
