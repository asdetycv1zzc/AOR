# WP-05 Migration Plan

Add immutable tool invocation metadata and artifact references before enabling broker writes. Existing MCP adapters remain read-only until descriptors and policy versions are present.
