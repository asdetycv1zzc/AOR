# Operations

Run deterministic checks in fresh Linux audit containers with hidden tests mounted read-only only to the runner. Keep Executor and Auditor worker pools separate, retain evidence for the configured policy period, and fail closed when the evidence store or signer is unavailable.
