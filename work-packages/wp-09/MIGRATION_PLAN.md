# Migration Plan

Adopt the service behind the existing Tool Broker repository capability. Existing direct workspace writers are removed after one release; historical commits remain immutable and are referenced by Submission Manifest hashes.
