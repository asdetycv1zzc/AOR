# WP-00 Migration Plan

This is the initial repository baseline, so no data migration is required. Later bootstrap changes must remain compatible with the supported Go toolchain and CI entry points. Renamed paths require a compatibility period or a recorded ADR.

Rollback consists of reverting the WP-00 commit. It does not alter external state or persistent data.
