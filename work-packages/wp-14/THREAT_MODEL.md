# Threat Model

Deployment validation rejects privileged mode, host networking, Docker socket mounts, plaintext secret-like environment values, Windows container/VM claims, and retention below the audit policy.
