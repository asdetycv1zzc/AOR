# Operations

Use secret-manager references, digest-pinned images, three control-plane replicas, isolated Executor/Auditor pools, PITR WAL archiving, and a scheduled restore drill. Never replace a failed deployment by editing immutable event history.
