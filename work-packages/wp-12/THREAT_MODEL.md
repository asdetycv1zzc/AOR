# Threat Model

Controls cover interface collisions, path ownership overlap, forged evidence, merge replay, tenant key confusion, and merge-executor failures. Protected-branch and signed provenance enforcement remain deployment gates.
