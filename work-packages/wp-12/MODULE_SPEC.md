# WP-12 Integration

WP-12 owns the protected merge queue, deterministic integration audit, conflict findings, and idempotent merge execution. It consumes immutable passing submissions and never edits a module outside the Repository Service.
