# Migration Plan

Route task integration transitions through this queue after WP-10 audit. Existing direct branch merges are disabled once protected-branch status checks and provenance verification are enabled.
