# 个人反思

## 1. 这个项目是什么？

这个项目的正式名称是“多智能体组织运行时”，简称`AOR`。本意上是解决多智能体协同开发时带来的各类问题，例如多个Agent之间的协同调用统一协议、不同Agent背后LLM的通信实现、单个Agent越权与权限控制等问题。此外，对于每一种特定的任务选择不同的LLM供应商也是该项目的核心出发点之一。

然而在与GPT讨论之后，我突然发现解决这一系列问题的前提下，再加上明确的分层与目标商议，这个项目实际上实现了所谓“一句话编程”的目标，即与目标层来回交流并形成一份较为完整的项目文档后，多个智能体便会按照要求分层次地完成整个项目，也就是我所谓的“许愿式编程平台”（这并不是一个正式的名称）。

GPT5.6-Sol在某些问题上过于保守与防御性，因此在初版SPEC中，其将该项目描述为了一个企业级别的多租户系统，并且包含有极其充足甚至繁复的安全边界与防御性条款。在发现该问题后，我强行让其改变方向，优先实现全链路而非安全性功能，并且强制将环境设为预定义的`TEST`设定以绕过它一开始编写的大量企业生产设计。

这些代码并没有被完全移除，因为GPT的实现确实没有错，如果要将该项目正式投入使用，这些安全措施是必不可少的；并且也可以预留给以后的进一步迭代。但对于一个课程项目来说，这还是太臃肿了。

这个项目在我的预想中应该是要采取类似自举`GNU Compiler Collection`时的`bootstrap`流程，即：

- 现有Harness实现一阶段产物
- 使用一阶段产物作为Harness，重新实现自身，即二阶段产物
- 验证二阶段产物的可用性

但时间不允许，并且token的消耗量也远超预期，因此只进行了一阶段，以后在有充足时间和资金的前提下可能会继续往后实现。

## 2. 关于 Superpowers

我是在 `GPT-5.6-Sol` 正式发布后才开始着手该项目。在我的理解中，Superpowers本质上承担了一部分后训练不足的代价，例如将其应用在`Deepseek-V4-Pro(Preview)`上可以有效地改善其模型本身后训练不足的问题。

然而，`GPT-5.6-Sol`作为我能够接触到且能承受的起的唯一的大语言模型，通过几天的试用（开发其他项目）后，我认为其后训练已经包含且内化了Superpowers的计划、TDD、Review等等操作，甚至可以说其启动的SubAgent几乎只会用来单元测试或审计（这也是我认为其防御性过重的一方面）。

因此，我认为在`GPT-5.6-Sol`套上Superpowers只会起到降低Token效率的作用，所以全程没有使用Superpowers框架。

## 3. 关于 TDD

如前文所言，TDD已经内化为了GPT编程时的一个必需步骤，因此我一开始没有多加干涉。然而，在一个连续跑了2天的Goal结束后，我惊恐地发现绝大部分Token都被无效地耗费在了编写单元测试-单元测试不通过-查找代码问题-修复代码并新增对应的单元测试用例-审计的循环上。这固然是按照课程要求完成，但因此带来的上下文膨胀与注意力丧失反而使得整个项目停滞不前。因此，我强制要求GPT先实现，后测试。事实上，它也出色地完成了这个项目。

## 4. 关于 Subagent

Codex对于Subagent的编排并没有问题——有问题的是主Agent。在项目初期，Codex还是按部就班地将任务细分后交给不同的Subagent处理。但是经过数轮上下文压缩和Agent往复通信后，其最终会选择将类似审计或单元测试等杂活丢给Subagent，自己跑去实现主要代码——但是上下文窗口不足以同时支撑数个Subagent的定期汇报和写代码的任务，因此会更快地触发上下文压缩，导致恶性循环。

在我的实测下，如果首Token稳定在2-5s，一轮上下文压缩后就会开始出现偏离目标的现象，大约一个小时后会出现大幅度偏移。哪怕给予了明确的指示，最好情况也只能支撑大约两个小时不偏离目标。

不过即便如此，只要主体代码框架完全实现，业务代码不是完全没写的情况下，派出多个Subagent分别对代码进行审计能够大幅提升效率，毕竟缝缝补补比起从0到1的突破还是简单得多。

## 5. 关于 SPEC 和 PLAN

我不认为我提出的PLAN会比GPT综合分析后得出的计划更好，并且SPEC内已经包含了这些计划，因此我在一开始没有给出PLAN。

和GPT网页端对话，再让其生成多智能体通用SPEC是我一直以来的Vibe Coding范式。GPT网页端在开启Web Search能够广泛撷取到大量信息（并且一周3000次，近乎免费）。虽然我一般会对SPEC进行审阅，但这是我在`5.6-Sol`推出后第一次生成SPEC，结果也是远超预期的——GPT吐出来了一份100多KB的文档，使得人力几乎无法逐行逐句审阅，也为之后的链路实现方向不明晰埋下了伏笔。

## 6. 有关 Prompt 和 Context 策略。

根据我的实验与Codex的源代码，Codex似乎会在对上下文压缩后再塞大量User Prompt进入到当前的新上下文中。而开启了Goal后，还会额外定期读取Goal的实时内容。因此，我将我先前使用过的`AGENTS.md`进行了整合与最小化，并通过发送Goal的方式让其始终读取最新的限制。虽然这事实上降低了Codex的可用上下文窗口，但也使其在指令遵守方面表现有所提高。

## 7. 有关凭据和分发

主要问题是分发引起的。按照我的构想，这个项目应该能在Linux和Windows上平台无关地运行。但是，一开始定下的Docker策略使得该项目在Windows上只有两种选择：

- 抛弃Docker的依赖处理与一键启动，全部从头构建一份裸的二进制并运行
- 在Docker运行，但Docker在Windows上实质是Hyper-V虚拟机

考虑到项目的依赖和规模，选择一几乎不可能（即使已经预留了相关的接口和代码），即该项目在Windows上推荐的运行方式仍然是Docker+Hyper-V，这会导致很多工具链调用的问题，例如无法调用MSVC工具链、MinGW可能不完整或需要非常复杂的构建步骤（在Aarch64上需要构建aarch64-w64-mingw32-gcc stage1工具链并用其进一步交叉编译）等等。

这些问题在目前的前提下没有好的解决方案，只能完整地实现SPEC要求，即完整实现跨平台一致的沙箱后才能算作解决。

## 8. 如果重来？

如果可以回到与GPT头脑风暴的那个时间点，我会强调这是一个课程项目（当时我并没有强调），从而减少大量的Token浪费（我估计GPT在这上面浪费了至少 $ 3000 $ 美元）与时间浪费，也能够缩小当前项目的规模，代价是如果以后想迭代该项目会变得更加费劲。

Superpowers 我仍然不会启用，原因见上，我在该项目开始时就对其进行了认真的考虑并作出了放弃的决断。

TDD 在目前各家LLM对于Coding大量堆后训练的背景下，我认为其也并非必要——人力这么做无可厚非，但对于等效Context和Coding智商方面远强于人类的LLM来说，这么做我怀疑只会降低LLM的有效上下文窗口与注意力，最终适得其反，但我没有验证过这个结论，因此只是持怀疑的态度。

项目要求的PR，我并不是很理解。我先前也一个人或多个人协作写过一些微型或中型（对于人力来说）的项目，多人时使用PR虽然有时会带来全局锁一样的效果，但大部分情况下起到的还是各自模块的锁的作用，可以有效提高协作的效率。

但是单人写项目，无论是先前GPT刚刚出世默默无闻时的古法编程，还是现在的全栈Vibe Coding，我都没有使用PR或要求Agent使用PR的习惯。当然，这一切的前提是中小项目。中大型项目我没有一个人编写的经验，但我想这时如果一个人编写项目，也是需要PR来减轻认知负担的。
