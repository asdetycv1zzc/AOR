apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "aor.serviceAccountName" . }}
  labels:
    app.kubernetes.io/name: {{ include "aor.name" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
automountServiceAccountToken: false
