# Local Compose

The local profile uses pinned image digests, file-backed secret references, read-only application roots, and no privileged or host-network services. Replace zero digests with release-manifest digests during deployment; unsigned images are not admitted to production.
