DO NOT send optional commentary.

# Agent 工作规范

本文件是 `AOR` 项目的工作规范，适用于在该项目中工作的所有智能体。

## 1. 规则优先级

- 如果你是 Codex，请忽略目录下的 `CLAUDE.md`，只遵守本 `AGENTS.md`。
- 本文件中的路径、工具和安全限制优先于智能体的默认工作习惯。

## 2. 工作区范围

- 只允许读取 Codex 二进制所在目录。
- 只允许在当前目录下的 `AOR` 目录中创建、修改、删除文件或修改文件权限。
- 严禁对上述范围以外的路径执行任何文件操作。

## 3. Git 工作流

- 开始任何工作前，先检查 Git 仓库、工作树是否存在以及工作树是否干净。
- 如果不存在 Git 工作树，以 Akimisaka（`m18662601115@163.com`）身份执行 `git init`。
- 如果 Git 工作树已存在，保留现有仓库和他人已有改动；所有提交均使用 Akimisaka（`m18662601115@163.com`）身份，且不得 push。
- 每完成一项独立任务，立即创建一个清晰、范围最小的 `git commit`。

## 4. 实现原则

- 禁止任何形式的作弊，例如硬编码参数绕过限制或硬编码测试样例绕过测试。
- 以正确实现需求为目标，确认代码逻辑确实满足要求，而不是只追求测试结果。
- 禁止过度设计：不要添加不必要的篡改检查、层层 wrapper、接口包装或单元测试。
- 在完整实现需求的前提下，保持接口和实现简单；只有能降低实际复杂度的抽象才应当引入。

## 5. 工具限制

- 优先使用 PowerShell，尽量避免 Bash。
- 严禁使用 `awk` 和 `printf`。
- 当命令包含引号时，严禁使用 `sed`、`awk`、`grep` 或 `printf`。
- 单个文件的写入、创建和删除必须使用 Read/Write 工具。
- 批量文件操作只能通过 Read/Write 工具临时创建 Python 脚本；执行前必须审计脚本中的字符转义。
- 除非用户明确要求，严禁使用 Windows 上安装的 MSYS2 工具链。
- 严禁启动任何虚拟机，包括但不限于 WSL、VMware 和 VirtualBox。

## 6. 危险操作

严禁执行以下操作及同等危险的变体：

- 重置或强制覆盖 Git 工作树。
- 强制推送。
- 批量删除文件。
- 关机或重启系统。
- 杀死进程。
