# Agent Roles

Agent runtime and role implementations live here. Agents submit typed requests and reports; they never write control-plane facts, create other Agents, merge source, or decide completion.
